import { Bot, UserRound } from 'lucide-react'
import { useCounselling } from '../../context/CounsellingContext'
export default function ChatMessage({ message }) { const { replayAnswer, stopAnswer, speaking } = useCounselling(); const isAssistant = message.role === 'assistant'; return <article className={`chat-message ${message.role}`}><div className="message-avatar">{isAssistant ? <Bot size={16} /> : <UserRound size={16} />}</div><div className="message-body"><div className="message-meta"><strong>{isAssistant ? 'Assistant' : 'You'}</strong><time>{new Date(message.timestamp).toLocaleTimeString([], { hour: '2-digit', minute: '2-digit' })}</time></div><p>{message.text}</p>{isAssistant && message.id !== 'welcome' && <div className="message-actions"><button onClick={speaking ? stopAnswer : replayAnswer} aria-label={speaking ? 'Stop answer' : 'Replay answer'}>{speaking ? '⏹ Stop' : '🔊 Replay'}</button></div>}</div></article> }
